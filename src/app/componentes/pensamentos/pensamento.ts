// interface para obrigar os tipos que deverão ser usados por cada propriedade de pensamento
export interface Pensamento {
    id?: number
    conteudo: string
    autoria: string
    modelo: string
}